<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:EcoCasa.ViewModel;assembly=EcoCasa"
             xmlns:behavior="clr-namespace:EcoCasa.Util;assembly=EcoCasa"
             x:Class="EcoCasa.Views.ProfilePage"
             Title="User Profile">

    <ContentPage.Resources>
        <ResourceDictionary>
            <viewModel:ProfileViewModel x:Key="ProfileViewModel"></viewModel:ProfileViewModel>
            <behavior:ProfileBehavior x:Key="ProfileBehavior" />
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <StackLayout BindingContext="{StaticResource ProfileViewModel}">
        <Label Text="{Binding Nome}"></Label>
        <Label Text="{Binding Email}"></Label>

        <ListView SeparatorVisibility="Default" ItemsSource="{Binding Casa}" ItemSelected="ListView_OnItemSelected" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding Address}"></TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <Button Text="Contacts" Command="{Binding Contacts}"></Button>
        <Button Text="Add new SmartCasa" Command="{Binding CreateSmartCasaCommand}"></Button>
        <Button Text="Logout" Command="{Binding LogOutCommand}"></Button>
    </StackLayout>
</ContentPage>